import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF0066FF); // Electric Blue
  static const secondary = Color(0xFF8B5CF6); // Vibrant Purple
  static const accent = Color(0xFF00FF88); // Neon Green
  static const danger = Color(0xFFFF4444);
  static const warning = Color(0xFFFFA500);
  
  // Dark theme colors
  static const darkBackground = Color(0xFF0A0E27);
  static const darkSurface = Color(0xFF1A1F3A);
  static const darkCard = Color(0xFF242B4A);
  
  // Light theme colors
  static const lightBackground = Color(0xFFF5F7FA);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightCard = Color(0xFFFFFFFF);
  
  // Text colors
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFB0B8D4);
  static const textLight = Color(0xFF64748B);
}
